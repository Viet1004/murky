---
name: dev
description: Start the extension in watch mode for development
---

Start the Murky extension in watch mode:

1. Run `npm run watch` in the project root
2. This starts esbuild in watch mode for all three entry points (content, popup, background)
3. Remind the user to reload the extension in `chrome://extensions` after changes
